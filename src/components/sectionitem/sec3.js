import React from 'react'
import "./secitem.css"

const sec3 = () => {
  return (
    <div className="sec-part">
    <div className="secitem-logo">
     <img src="" alt="tag" />
     </div>
     <div className="sec-line">
     <h4>Performant</h4>
   </div>
   <div className="sec-para">
   <p>In most cases, the connection will be established with WebSocket, providing a low-overhead communication channel between the server and the client.</p>
   </div> 
   </div>
  )
}

export default sec3
